<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Recent Errors"
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Close button (subtle) -->
        <Style x:Key="CloseButton" TargetType="Button">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="#3E3E3E" CornerRadius="4" Padding="20,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4E4E4E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2E2E2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Clear button (soft red) -->
        <Style x:Key="ClearButton" TargetType="Button">
            <Setter Property="Foreground" Value="#1E1E1E"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="#FF6B6B" CornerRadius="4" Padding="20,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF8787"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF5555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="8" BorderBrush="#FF6B6B" BorderThickness="2">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with icon and title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <!-- Warning icon -->
                <Border Width="40" Height="40" CornerRadius="20" Background="#FF6B6B" Margin="0,0,14,0">
                    <TextBlock Text="&#x26A0;" FontSize="24" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Recent Errors" FontSize="16" FontWeight="SemiBold"
                               Foreground="#E0E0E0"/>
                    <TextBlock Text="Errors encountered during this replication run" FontSize="11"
                               Foreground="#808080" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Error List -->
            <Border Grid.Row="1" Background="#2D2D2D" BorderBrush="#3E3E3E" BorderThickness="1"
                    CornerRadius="4" Margin="0,0,0,20">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ListBox x:Name="lstErrors" Background="Transparent" BorderThickness="0"
                             Foreground="#E0E0E0" Padding="10">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#252525" CornerRadius="4" Padding="12,8" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Timestamp -->
                                        <TextBlock Grid.Column="0" Text="{Binding Timestamp}"
                                                   Foreground="#808080" FontFamily="Consolas" FontSize="11"
                                                   VerticalAlignment="Top" Margin="0,0,12,0"/>

                                        <!-- Error Message -->
                                        <TextBlock Grid.Column="1" Text="{Binding Message}"
                                                   Foreground="#FFB340" TextWrapping="Wrap" FontSize="12"
                                                   VerticalAlignment="Top"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnClear" Content="Clear All" Style="{StaticResource ClearButton}"
                        Margin="0,0,10,0" ToolTip="Clear error history and reset counter"/>
                <Button x:Name="btnClose" Content="Close" Style="{StaticResource CloseButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
