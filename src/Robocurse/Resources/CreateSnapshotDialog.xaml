<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create VSS Snapshot"
        Width="420" Height="320"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Primary action button (blue) -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="#0078D4" CornerRadius="4" Padding="20,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A8FE0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#006CBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Cancel button (subtle) -->
        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="#3E3E3E" CornerRadius="4" Padding="20,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4E4E4E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2E2E2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#1E1E1E" CornerRadius="8" BorderBrush="#3E3E3E" BorderThickness="1">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with icon and title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <!-- Camera/snapshot icon -->
                <Border Width="40" Height="40" CornerRadius="20" Background="#0078D4" Margin="0,0,14,0">
                    <TextBlock Text="&#x1F4F7;" FontSize="20" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Create VSS Snapshot" FontSize="16" FontWeight="SemiBold" Foreground="#E0E0E0"/>
                    <TextBlock Text="Create a new Volume Shadow Copy" FontSize="11" Foreground="#808080" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Server Selection -->
            <Grid Grid.Row="1" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Server:" Foreground="#B0B0B0" VerticalAlignment="Center" FontSize="13"/>
                <ComboBox Grid.Column="1" x:Name="cmbDialogServer" FontSize="13" Padding="8,6"
                          Background="#2D2D2D" Foreground="#E0E0E0" BorderBrush="#3E3E3E">
                    <ComboBoxItem Content="Local" IsSelected="True"/>
                </ComboBox>
            </Grid>

            <!-- Volume Selection -->
            <Grid Grid.Row="2" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Volume:" Foreground="#B0B0B0" VerticalAlignment="Center" FontSize="13"/>
                <ComboBox Grid.Column="1" x:Name="cmbDialogVolume" FontSize="13" Padding="8,6"
                          Background="#2D2D2D" Foreground="#E0E0E0" BorderBrush="#3E3E3E"/>
            </Grid>

            <!-- Retention Option -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
                <CheckBox x:Name="chkEnforceRetention" Content="Enforce retention before creating"
                          Foreground="#E0E0E0" IsChecked="True" VerticalAlignment="Center" FontSize="13"/>
                <TextBox x:Name="txtRetentionCount" Text="3" Width="45" Margin="12,0,8,0"
                         Background="#2D2D2D" Foreground="#E0E0E0" BorderBrush="#3E3E3E"
                         TextAlignment="Center" VerticalContentAlignment="Center" FontSize="13" Padding="4"/>
                <TextBlock Text="snapshots" Foreground="#B0B0B0" VerticalAlignment="Center" FontSize="13"/>
            </StackPanel>

            <!-- Status Message -->
            <Border Grid.Row="4" Background="#2D2D2D" CornerRadius="4" Padding="12" Margin="0,16,0,0">
                <TextBlock x:Name="txtDialogStatus" Foreground="#808080" TextWrapping="Wrap" FontSize="12"/>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="btnDialogCancel" Content="Cancel" Style="{StaticResource CancelButton}" Margin="0,0,10,0"/>
                <Button x:Name="btnDialogCreate" Content="Create" Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
